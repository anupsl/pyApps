from src.modules.iris.campaigns import campaigns

class Test_Benchmark():
    '''
    def test_createCampaign_benchmark(self,benchmark):
        
        def setup():
            return {'payloadData':{'name':'IRIS_' + str(int(time.time()))}}
        benchmark.pedantic(campaigns.createCampaign(), args=(1, 2, 3), kwargs={'foo': 'bar'}, iterations=10, rounds=100)
    '''